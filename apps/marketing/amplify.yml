version: 1
applications:
  - appRoot: apps/marketing
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 22
            - nvm use 22
            - cd ../.. && npm ci
        build:
          commands:
            - cd ../.. && npm run build --workspace=www
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
          - .next/cache/**/*
