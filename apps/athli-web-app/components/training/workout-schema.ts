/**
 * Workout Schema - Web App
 *
 * Re-exports all shared types from @athli/shared-types.
 * IMPORTANT: Do NOT duplicate types here - import from shared package instead.
 */

// Re-export all shared types
export * from '@athli/shared-types';
