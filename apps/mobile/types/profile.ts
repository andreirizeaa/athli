import type {
  CoachProfile,
  ClientProfile,
  ProfileType,
  AuthResult,
} from '@athli/shared-types';

// Re-export types from shared-types for backwards compatibility
export type {
  CoachProfile,
  ClientProfile,
  ProfileType,
  AuthResult,
};
