import type {
  CoachProfile,
  ClientProfile,
  ProfileType,
  AuthResult,
  CoachAssignment,
} from '@athli/shared-types';

// Re-export types from shared-types for backwards compatibility
export type {
  CoachProfile,
  ClientProfile,
  ProfileType,
  AuthResult,
  CoachAssignment,
};
