// Placeholder for DB client
// When you choose a DB (e.g., Postgres + Prisma, Mongo + Mongoose),
// implement the client here as a singleton.

export const db = {
  // DB client will be initialized here
};

